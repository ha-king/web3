version: 0.2
phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - npm install -g aws-cdk
      - ls -la
      - npm install
  build:
    commands:
      - npm run build
      - npx cdk deploy Web3ProdStack --require-approval never